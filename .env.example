# Portainer Configuration
# URL to your Portainer instance
PORTAINER_URL=http://192.168.0.242:9000

# Portainer API token (get from Portainer UI: User settings -> Access tokens)
# Format: ptr_xxxxxxxxxxxx
PORTAINER_API_TOKEN=ptr_your_portainer_api_token_here

# MCP Wrapper Configuration
# Token for authenticating HTTP requests to the wrapper (generate with: openssl rand -base64 32)
# This should be a strong random token (minimum 32 characters)
MCP_ACCESS_TOKEN=your_secure_random_mcp_access_token_here

# Port to expose the MCP HTTP/SSE endpoint
MCP_PORT=8080

# Optional: Path to custom tools configuration file
# MCP_TOOLS_FILE=/config/tools.yaml

# Optional: Disable Portainer version compatibility check
# DISABLE_VERSION_CHECK=false

# Optional: Enable read-only mode for safety (all write operations will be blocked)
# READ_ONLY_MODE=false

# Optional: Path to Portainer MCP binary (default: /app/portainer-mcp in container)
# MCP_BINARY_PATH=/app/portainer-mcp
